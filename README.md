# ترتيلا (Tartila)

ترتيلا — مصحف رقمي، تفسير، وأذكار.

## التشغيل

```bash
flutter pub get
flutter run
```

## iOS (للمطوّر)

```bash
cd ios && pod install && cd ..
```

ثم افتح `ios/Runner.xcworkspace` في Xcode.
